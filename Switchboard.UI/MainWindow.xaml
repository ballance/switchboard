<Window x:Class="Switchboard.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}"
        Title="Local Services Monitor" Height="350" Width="525">
    <Grid>
        <StackPanel>
            <TextBlock Name="TestUrl" Padding="5" Text="{Binding TestUrl, Mode=OneWay}"/>
            <Rectangle Fill="Orange" Width="Auto" Height="2"></Rectangle>
            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                <TextBlock Name="StatusDisplay" Text="{Binding Status, Mode=OneWay}" Padding="5" Height="Auto" />
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Window>